package com.lam.graphics.sprites;

/**
 * SpriteState is the allowed states of a Sprite.
 */
enum SpriteState {
    // The object is currently inactive
    Inactive,
    // The object is currently active
    Active,
    // The object is finished and ready to be deactivated
    Complete
}
